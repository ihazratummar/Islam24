ackage com.example.zakat_calculator


import android.annotation.SuppressLint
import android.app.Activity
import android.content.Intent
import android.os.Bundle
import android.os.Handler
import android.os.Looper
import android.view.View
import android.widget.Button
import android.widget.ImageView
import android.widget.TextView
import androidx.databinding.DataBindingUtil.setContentView
import androidx.fragment.app.Fragment
import com.google.android.gms.location.LocationServices
import java.util.Calendar
import java.util.TimeZone
import java.text.SimpleDateFormat as SimpleDateFormat1


class HomePage : Fragment() {
    private lateinit var _bgHomePageEk2: View
    private lateinit var _bgHButtomBoxEk1: View
    private lateinit var hButtomWhite: View
    private lateinit var hHomeButton: ImageView
    private lateinit var hTimeButton: ImageView

    private lateinit var hProfileButton: ImageView
    private lateinit var hHomeButtonText: TextView
    private lateinit var hLiveAboveMenubar: ImageView
    //	private lateinit var _bgHTDPZButtonsEk1: View
    private lateinit var _bgHZakatButtonEk1: View
    private lateinit var hZakatBox: View
    private lateinit var hZakatIcon: ImageView
    private lateinit var hZakatText: TextView
    //	private lateinit var _bgHPrayerTimeButtonEk1: View
    private lateinit var hPrayerBox: View
    private lateinit var hPrayerIcon: ImageView
    private lateinit var hPrayerTimeText: TextView
    private lateinit var _bgHDuaButtonEk1: View
    private lateinit var hDuoBox: View
    private lateinit var hDuaIcon: ImageView
    private lateinit var hDuasText: TextView
    private lateinit var _bgHTasbihButtonEk1: View
    private lateinit var hTasbihBox: ImageView
    private lateinit var hTasbihIcon: ImageView
    private lateinit var hTasbihText: TextView
    private lateinit var _bgHSurahBoxEk1: View
    private lateinit var hSurahBoxGreen: View
    private lateinit var hHalfMoonBox: ImageView
    private lateinit var hLastRead: TextView
    private lateinit var hSurahName: TextView
    private lateinit var hIslamMoon: ImageView
    private lateinit var roundedRectangle: View
    private lateinit var _timeLocationWhiteBox: View
    private lateinit var ellipse3: View
    private lateinit var vector1: ImageView
    private lateinit var salatWaqt: TextView
    private lateinit var locationIcon: ImageView
    private lateinit var yourLocation: TextView
    private lateinit var lineUnderTimeLocation: ImageView
    private lateinit var salatTimetextView:TextView
    private lateinit var fusedLocationProviderClient: FusedLocationProviderClient
    private lateinit var locationNameTextView:TextView



    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.fragment_home)


        hButtomWhite = findViewById(R.id.h_buttom_white)
        hHomeButton = findViewById(R.id.h_home_button)
        hTimeButton = findViewById(R.id._h_time_button)
        hProfileButton = findViewById(R.id.h_profile_button)
        hHomeButtonText = findViewById(R.id.h_home_button_text)
        hLiveAboveMenubar = findViewById(R.id.h_live_above_menubar)
        _bgHZakatButtonEk1 = findViewById(R.id._h_zakat_button_shape)
        hZakatBox = findViewById(R.id.h_zakat_box)
        hZakatIcon = findViewById(R.id.h_zakat_icon)
        hZakatText = findViewById(R.id.h_zakat_text)
        hPrayerBox = findViewById(R.id.h_prayer_box)
        hPrayerIcon = findViewById(R.id.h_prayer_icon)
        hPrayerTimeText = findViewById(R.id.h_prayer_time_text)
        _bgHDuaButtonEk1 = findViewById(R.id._h_duas_button_shape)
        hDuoBox = findViewById(R.id.h_duo_box)
        hDuaIcon = findViewById(R.id.h_dua_icon)
        hDuasText = findViewById(R.id.h_duas_text)
        _bgHTasbihButtonEk1 = findViewById(R.id._h_tasbih_button_shape)
        hTasbihBox = findViewById(R.id.h_tasbih_box)
        hTasbihIcon = findViewById(R.id.h_tasbih_icon)
        hTasbihText = findViewById(R.id.h_tasbih_text)
        _bgHSurahBoxEk1 = findViewById(R.id._bg__h_surah_box_ek1)
        hSurahBoxGreen = findViewById(R.id.h_surah_box_green)
        hHalfMoonBox = findViewById(R.id.h_half_moon_box)
        hLastRead = findViewById(R.id.h_last_read)
        hSurahName = findViewById(R.id.h_surah_name)
        hIslamMoon = findViewById(R.id.h_islam_moon)
        roundedRectangle = findViewById(R.id.rounded_rectangle)
        _timeLocationWhiteBox = findViewById(R.id._time_location_white_box)
        ellipse3 = findViewById(R.id.ellipse_3)
        vector1 = findViewById(R.id.vector_1)
        salatWaqt = findViewById(R.id.salat_waqt)
        locationIcon = findViewById(R.id.location_icon)
        yourLocation = findViewById(R.id.your_location)
        lineUnderTimeLocation = findViewById(R.id.line_under_time_location)
        salatTimetextView = findViewById(R.id.h_salat_timer)
        fusedLocationProviderClient = LocationServices.getFusedLocationProviderClient(this)
        locationNameTextView = findViewById(R.id.loaction_name)

        updateCurrentTime()


        ///Current Timer Start
        val handler = Handler(Looper.getMainLooper())
        handler.postDelayed(object : Runnable{
            override fun run() {
                updateCurrentTime()
                handler.postDelayed(this, 1000)
            }
        }, 1000)

        // custom code goes here
    }

    private fun setContentView(fragmentHome: Int) {

    }

    private fun updateCurrentTime(){
        val currentTime = getCurrentTime()
        salatTimetextView.text = currentTime
    }

    @SuppressLint("SimpleDateFormat")
    private fun getCurrentTime():String{
        val calendar = Calendar.getInstance(TimeZone.getDefault())
        val simpleDateFormat = SimpleDateFormat1("HH:mm:ss")
        return simpleDateFormat.format(calendar.time)
    }

    ///Current Timer End

    /// Current Location Start

    /// Current Location End


}
